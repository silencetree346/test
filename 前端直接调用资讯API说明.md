# 前端直接调用资讯API说明 🌐

## ✅ 已实现的资讯源（无需后端）

### 1. The Guardian API（国际资讯）⭐

**特点：**
- ✅ 无需后端，直接调用
- ✅ 使用公共测试Key `test`
- ✅ 免费，无配额限制
- ✅ 支持CORS，可在GitHub Pages使用

**API地址：**
```
https://content.guardianapis.com/search
```

**参数：**
- `q`: 搜索关键词（coffee OR starbucks OR cafe）
- `from-date`: 开始日期
- `to-date`: 结束日期
- `page-size`: 返回数量
- `api-key`: test（公共Key）

**已实现：** ✅ 已集成到代码中

---

### 2. RSS Feed + CORS代理（国内资讯）⭐

**特点：**
- ✅ 无需后端，通过CORS代理访问RSS
- ✅ 免费，无配额限制
- ✅ 支持多个国内资讯源

**使用的CORS代理：**
```
https://api.allorigins.win/raw?url=
```

**已集成的RSS源：**

#### 2.1 36氪 RSS
- **RSS地址：** `https://36kr.com/feed`
- **类型：** 科技创投
- **过滤：** 自动过滤咖啡/奶茶相关资讯
- **状态：** ✅ 已集成

#### 2.2 虎嗅 RSS
- **RSS地址：** `https://www.huxiu.com/rss/0.xml`
- **类型：** 商业分析
- **过滤：** 自动过滤咖啡/奶茶相关资讯
- **状态：** ✅ 已集成

#### 2.3 钛媒体 RSS
- **RSS地址：** `https://www.tmtpost.com/rss.xml`
- **类型：** 科技媒体
- **过滤：** 自动过滤咖啡/奶茶相关资讯
- **状态：** ✅ 已集成

#### 2.4 界面新闻 RSS
- **RSS地址：** `https://www.jiemian.com/rss.xml`
- **类型：** 商业新闻
- **过滤：** 自动过滤咖啡/奶茶相关资讯
- **状态：** ✅ 已集成

---

## 🔍 其他可用的公开API（可选）

### 3. NewsAPI（需要API Key）

**特点：**
- ⚠️ 免费版有CORS限制
- ⚠️ 需要HTTPS环境
- ⚠️ GitHub Pages可以，但需要API Key

**注册地址：** https://newsapi.org/

**限制：**
- 免费版：100次请求/天
- 需要API Key
- 部分源需要付费

**状态：** ❌ 未集成（需要API Key）

---

### 4. RSS Feed 其他源（可添加）

以下RSS源也可以添加，只需修改代码：

#### 4.1 澎湃新闻
```
https://www.thepaper.cn/rss.xml
```

#### 4.2 财新网
```
https://www.caixin.com/rss.xml
```

#### 4.3 第一财经
```
https://www.yicai.com/rss.xml
```

#### 4.4 36氪其他频道
```
https://36kr.com/feed/tech
https://36kr.com/feed/business
```

---

## 📊 当前获取的资讯源总结

| 资讯源 | 类型 | 方式 | 状态 |
|--------|------|------|------|
| The Guardian | 国际 | API直接调用 | ✅ 已集成 |
| 36氪 | 国内 | RSS + CORS代理 | ✅ 已集成 |
| 虎嗅 | 国内 | RSS + CORS代理 | ✅ 已集成 |
| 钛媒体 | 国内 | RSS + CORS代理 | ✅ 已集成 |
| 界面新闻 | 国内 | RSS + CORS代理 | ✅ 已集成 |

**总计：** 5个资讯源，全部无需后端！

---

## 🚀 使用方法

### 自动获取

代码会自动：
1. ✅ 尝试从后端获取（如果已配置）
2. ✅ 通过RSS Feed获取国内资讯
3. ✅ 通过The Guardian API获取国际资讯
4. ✅ 自动过滤咖啡/奶茶相关资讯
5. ✅ 合并所有资讯并分类显示

### 手动触发

点击页面上的"获取最新资讯"按钮即可。

---

## ⚙️ 工作原理

### RSS Feed方案

```
前端代码
  ↓
CORS代理 (api.allorigins.win)
  ↓
RSS Feed (36kr.com/feed)
  ↓
解析XML
  ↓
过滤关键词（咖啡/奶茶）
  ↓
显示资讯
```

### The Guardian API方案

```
前端代码
  ↓
直接调用API (content.guardianapis.com)
  ↓
返回JSON数据
  ↓
过滤关键词
  ↓
显示资讯
```

---

## 🔧 添加更多RSS源

### 步骤1：找到RSS地址

访问目标网站，查找RSS链接，通常在：
- 网站底部
- `/rss` 或 `/feed` 路径
- 浏览器地址栏输入 `网站域名/rss.xml` 或 `/feed`

### 步骤2：添加到代码

在 `fetchChineseNews()` 方法中添加：

```javascript
// 新RSS源
try {
    const rssNew = await axios.get(
        corsProxy + encodeURIComponent('RSS地址'), 
        { timeout: 5000 }
    );
    const parser = new DOMParser();
    const xmlDoc = parser.parseFromString(rssNew.data, 'text/xml');
    const items = xmlDoc.querySelectorAll('item');
    
    items.forEach(item => {
        const title = item.querySelector('title')?.textContent || '';
        const link = item.querySelector('link')?.textContent || '#';
        const pubDate = item.querySelector('pubDate')?.textContent || '';
        const description = item.querySelector('description')?.textContent || '';
        
        // 过滤关键词
        if (title.toLowerCase().includes('咖啡') || 
            title.toLowerCase().includes('奶茶') ||
            description.toLowerCase().includes('咖啡') ||
            description.toLowerCase().includes('奶茶')) {
            allNews.push({
                title: title,
                url: link,
                source: '资讯源名称',
                publish_date: pubDate ? new Date(pubDate).toISOString().split('T')[0] : new Date().toISOString().split('T')[0],
                summary: description.substring(0, 150) || title
            });
        }
    });
    console.log(`✅ 从新RSS源获取资讯`);
} catch (e) {
    console.log('ℹ️ 新RSS源获取失败:', e.message);
}
```

---

## ⚠️ 注意事项

### CORS代理限制

- **api.allorigins.win** 是公共代理，可能有速率限制
- 如果代理失效，可以更换其他CORS代理：
  - `https://cors-anywhere.herokuapp.com/`
  - `https://api.codetabs.com/v1/proxy?quest=`

### RSS Feed限制

- 不是所有网站都提供RSS
- RSS内容可能不完整
- 需要手动过滤关键词

### 关键词过滤

当前过滤的关键词：
- 咖啡相关：咖啡、星巴克、瑞幸、cafe、coffee
- 奶茶相关：奶茶、bubble tea、boba、milk tea

可以根据需要调整过滤逻辑。

---

## 🎯 优化建议

### 1. 添加更多关键词

在过滤逻辑中添加更多品牌和行业词：
```javascript
const keywords = [
    '咖啡', '奶茶', '星巴克', '瑞幸', 'Manner', 'Seesaw',
    'coffee', 'tea', 'starbucks', 'luckin', 'cafe'
];
```

### 2. 优化CORS代理

如果 `api.allorigins.win` 不稳定，可以：
- 使用多个代理服务，失败时切换
- 部署自己的CORS代理（需要后端）

### 3. 添加缓存

RSS Feed数据可以缓存更长时间（比如30分钟），减少请求次数。

---

## 📋 总结

**当前状态：**
- ✅ 5个资讯源已集成
- ✅ 全部无需后端
- ✅ 可直接在GitHub Pages使用
- ✅ 自动过滤相关资讯

**优势：**
- 🚀 无需部署后端
- 💰 完全免费
- ⚡ 快速获取
- 🌐 支持GitHub Pages

**限制：**
- ⚠️ RSS内容可能不完整
- ⚠️ CORS代理可能有速率限制
- ⚠️ 需要手动过滤关键词

---

**现在你的网站可以获取国内外多个资讯源的真实数据了！** 🎉✨

