# 🔧 分支选择问题解决指南

## ❌ 问题：Branch没有地方可以选择main

**原因：** 代码还没有推送到GitHub，所以GitHub Pages设置页面看不到分支选项。

---

## ✅ 解决方案

### 步骤1：推送代码到GitHub

**必须先推送代码，GitHub Pages才能看到分支！**

在终端执行：

```bash
cd /Users/luckincoffee/test

# 初始化Git（如果还没有）
git init
git branch -M main

# 配置远程仓库
git remote remove origin
git remote add origin https://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@github.com/silencetree346/tongxu.git

# 确保文件最新
cp tongxu/实时资讯.html docs/index.html
touch docs/.nojekyll

# 添加、提交、推送
git add .
git commit -m "Deploy: 部署到GitHub Pages"
git push -u origin main
```

**或者运行脚本：**
```bash
chmod +x 检查并推送代码.sh
./检查并推送代码.sh
```

---

### 步骤2：刷新GitHub Pages设置页面

1. **推送代码后**，访问：
   ```
   https://github.com/silencetree346/tongxu/settings/pages
   ```

2. **刷新页面**（按 `F5` 或 `Cmd+R`）

3. **现在应该可以看到**：
   - Source下拉菜单
   - Branch下拉菜单（显示 `main`）
   - Folder下拉菜单

---

### 步骤3：配置GitHub Pages

如果现在可以看到分支选项了：

1. **Source**：选择 `Deploy from a branch`
2. **Branch**：选择 `main`（现在应该可以看到）
3. **Folder**：选择 `/docs` ⚠️ **必须选择这个！**
4. 点击 **Save**

---

## 🔍 如果仍然看不到分支选项

### 检查1：确认代码已推送

访问：https://github.com/silencetree346/tongxu

查看是否有文件，特别是：
- `docs/index.html`
- `README.md`
- 其他文件

**如果没有文件**，说明代码还没有推送成功。

---

### 检查2：确认仓库不是空的

如果仓库显示 "This repository is empty"，需要先推送代码。

---

### 检查3：检查分支名称

可能您的默认分支是 `master` 而不是 `main`。

**检查方法：**
访问：https://github.com/silencetree346/tongxu

查看页面顶部显示的分支名称。

**如果是 `master`：**
- 在GitHub Pages设置中，Branch选择 `master`
- 或者将分支重命名为 `main`：
  ```bash
  git branch -M main
  git push -u origin main
  ```

---

## 📋 完整操作流程

### 1. 推送代码（必须第一步）

```bash
cd /Users/luckincoffee/test
git init
git branch -M main
git remote add origin https://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@github.com/silencetree346/tongxu.git
cp tongxu/实时资讯.html docs/index.html
touch docs/.nojekyll
git add .
git commit -m "Deploy to GitHub Pages"
git push -u origin main
```

### 2. 验证代码已推送

访问：https://github.com/silencetree346/tongxu

确认能看到文件。

### 3. 配置GitHub Pages

访问：https://github.com/silencetree346/tongxu/settings/pages

**刷新页面后**，应该可以看到：
- Source: `Deploy from a branch`
- Branch: `main`（或 `master`）
- Folder: `/docs`

### 4. 保存并等待

点击 Save，等待1-2分钟。

---

## ⚠️ 重要提示

**GitHub Pages设置页面只有在代码推送到GitHub后，才会显示分支选项！**

如果看不到分支选项，说明：
1. ❌ 代码还没有推送
2. ❌ 或者推送失败了

**解决方法：**
1. ✅ 先推送代码（使用上面的命令）
2. ✅ 刷新GitHub Pages设置页面
3. ✅ 现在应该可以看到分支选项了

---

## 🎯 快速检查

**推送代码后，访问以下链接确认：**

1. **仓库页面**（应该有文件）：
   ```
   https://github.com/silencetree346/tongxu
   ```

2. **docs文件夹**（应该有index.html）：
   ```
   https://github.com/silencetree346/tongxu/tree/main/docs
   ```

3. **Pages设置**（应该可以看到分支选项）：
   ```
   https://github.com/silencetree346/tongxu/settings/pages
   ```

---

## 🆘 如果推送失败

如果 `git push` 失败，可能的原因：

1. **需要认证**：使用Token作为密码
2. **网络问题**：检查网络连接
3. **Token过期**：重新生成Token

**解决方法：**
- 确保使用Token：`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`
- 或者重新生成Token：https://github.com/settings/tokens


